import * as React from 'react';
/**
 * Layout component that injects margin and padding in accordance with Recess size values.
 * Props for all padding and margin permutations are available in longhand and shorthand.
 *
 * Note: The accepted values relate to pixels, and the component convert px to rem behind the scenes.
 * E.g. <Spacer padding={8} /> will render a 'div' with the style 'padding: 0.5rem;'.
 *
 * Basic examples:
 * ```jsx
 * // with children
 * <Spacer padding={8}>{children}</Spacer>
 * <Spacer margin={8}>{children}</Spacer>
 *
 * // without children
 * <Spacer padding={8} />
 * <Spacer margin={8} />
 * ```
 *
 * Examples with horizontal props:
 * ```jsx
 * <Spacer paddingX={8} />
 * <Spacer marginX={8} />
 *
 * <Spacer paddingLeft={8} paddingRight={8} />
 * <Spacer marginLeft={8} marginRight={8} />
 * ```
 *
 * Examples with vertical props:
 * ```jsx
 * <Spacer paddingY={8} />
 * <Spacer marginY={8} />
 *
 * <Spacer paddingTop={8} paddingBottom={8} />
 * <Spacer marginTop={8} marginBottom={8} />
 * ```
 *
 * Examples with shorthand props:
 * ```jsx
 * // padding x, y
 * <Spacer px={8} py={8} />
 *
 * // margin x, y
 * <Spacer mx={8} my={8} />
 *
 * // padding top, bottom, left, right
 * <Spacer pt={8} pb={8} pl={8} pr={8} />
 *
 * // margin top, bottom, left, right
 * <Spacer mt={8} mb={8} ml={8} mr={8} />
 * ```
 *
 * If needed, the Spacer can also be rendered as an inline 'span':
 * ```jsx
 * <Spacer pr={8} tag='span' />
 * ```
 *
 * @packageDocumentation
 */
declare type Spacing = 4 | 8 | 12 | 16 | 20 | 24 | 28 | 32 | 36 | 40 | 44 | 48 | 56 | 64 | 72 | 80 | 88 | 96 | 104 | 112;
interface Margin {
    margin?: Spacing;
    marginX?: Spacing;
    mx?: Spacing;
    marginY?: Spacing;
    my?: Spacing;
    marginTop?: Spacing;
    mt?: Spacing;
    marginBottom?: Spacing;
    mb?: Spacing;
    marginLeft?: Spacing;
    ml?: Spacing;
    marginRight?: Spacing;
    mr?: Spacing;
}
interface Padding {
    padding?: Spacing;
    paddingX?: Spacing;
    px?: Spacing;
    paddingY?: Spacing;
    py?: Spacing;
    paddingTop?: Spacing;
    pt?: Spacing;
    paddingBottom?: Spacing;
    pb?: Spacing;
    paddingLeft?: Spacing;
    pl?: Spacing;
    paddingRight?: Spacing;
    pr?: Spacing;
}
export interface SpacerProps extends Margin, Padding {
    tag?: 'div' | 'span';
    children?: React.ReactNode;
    testid?: string;
}
export declare const Spacer: React.FC<SpacerProps>;
export {};
